export class StringHasher {
    static hash(string: string) {
        // TODO: hash string
        return string;
    }

    static compareWithHashedString(string: string, hashedString: string) {
        // TODO: compare it with used hash
        return string === hashedString
    }
}